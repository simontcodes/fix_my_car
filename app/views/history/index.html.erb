<h2>History</h2>

<% @completed_jobs.each do |job| %>
<%= link_to "#{job.car.client.name} #{job.car.model} #{job.created_at}", job_path(job) %>
<% end %>

<p> <%= link_to 'Back to main', root_path %> </p>
